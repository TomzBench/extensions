[package]
name = "audio-stt"
version = "0.1.0"
edition = "2021"

[lib]
name = "_stt"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.22", features = ["extension-module"] }
stt = { path = "../../../crates/stt" }
